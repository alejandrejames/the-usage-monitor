{"default":{"identifier":"default","description":"Lets the popover call the app's own commands and receive state updates. Tauri v2 denies all IPC unless a capability grants it, so without this the popover renders its default state forever.","local":true,"windows":["main"],"permissions":["core:default","core:event:allow-listen","core:event:allow-unlisten","notification:default","core:window:allow-set-size","core:window:allow-inner-size","core:window:allow-scale-factor"]}}